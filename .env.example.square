# Square
# Set SQUARE_ENV to "sandbox" while testing, "production" when live.
SQUARE_ENV=sandbox
SQUARE_ACCESS_TOKEN=REPLACE_ME
SQUARE_LOCATION_ID=REPLACE_ME

# Optional (recommended for production webhooks)
SQUARE_WEBHOOK_SIGNATURE_KEY=REPLACE_ME

# Your public base URL for building links in emails/logs (optional)
PUBLIC_BASE_URL=http://localhost:3000
